add_subdirectory(abcrypto)
if (UNIX)
  add_subdirectory(DeathHandler)
endif(UNIX)

add_subdirectory(EASTL)
add_subdirectory(EASTL/test/packages/EABase)
if (CMAKE_CXX_COMPILER_ID MATCHES "GNU")
    # Alta!
    target_compile_options(EASTL PRIVATE -Wno-maybe-uninitialized -Wno-sign-compare -Wno-unused-function -Wno-class-memaccess -Wno-unused-local-typedefs -Wno-extra)
    target_compile_options(EASTL PUBLIC -Wno-unused-variable -Wno-deprecated-copy -Wno-shift-negative-value -Wno-unused-parameter)
#    target_compile_options(EAStdC PRIVATE -Wno-extra -Wno-tautological-compare)
elseif(MSVC)
    # TODO: Silence warnings
endif()

if (ABX_SCENE_VIEWER)
  add_subdirectory(freeglut)
  add_subdirectory(glew)
endif(ABX_SCENE_VIEWER)
add_subdirectory(libless)
if (WIN32)
    # On Unix use installed version
    add_subdirectory(lua)
endif()
add_subdirectory(lz4)
if (WIN32)
#  add_subdirectory(PDCurses)
endif(WIN32)
add_subdirectory(PugiXml)

set(RECASTNAVIGATION_STATIC ON)
add_subdirectory(recastnavigation/Detour)
if (CMAKE_CXX_COMPILER_ID MATCHES "GNU")
    target_compile_options(Detour PRIVATE -Wno-class-memaccess)
endif()
add_subdirectory(recastnavigation/DetourTileCache)
add_subdirectory(recastnavigation/Recast)

add_subdirectory(tinyexpr)
