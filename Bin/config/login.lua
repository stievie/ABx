--------------------------------------------------------------------------------
-- Login Settings --------------------------------------------------------------
--------------------------------------------------------------------------------
-- how many logins attempts until ip is temporary disabled
-- set to 0 to disable
login_tries = 5

-- how long the retry timeout until a new login can be made (without disabling the ip)
login_retrytimeout = 5000

-- DH keys
server_keys = "abserver.dh"
